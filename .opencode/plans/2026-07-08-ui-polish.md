# UI Polish: upload_form.html Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Modernisasi tampilan `upload_form.html` — gradient background, card accent, custom file drop area, file list preview — tanpa mengubah logic atau menambah dependency baru.

**Architecture:** Semua perubahan di satu file `templates/upload_form.html`. CSS inline di `<style>`, JS diperluas sedikit untuk file list preview. Tidak ada file baru, tidak ada dependency baru.

## Global Constraints

- Satu file saja yang diubah: `templates/upload_form.html`
- Tidak boleh menambah CDN/dependency baru
- Logic form submit, flash message, dan SweetAlert tidak berubah
- `accept=".pdf,.jpg,.jpeg,.png"` dan `name="pdf_files"` tetap sama persis
- Tetap responsive via Bootstrap grid

---

### Task 1: Ganti blok `<style>`

**Files:** `templates/upload_form.html:10-23`

- [ ] Ganti seluruh blok `<style>` dengan CSS baru: gradient background, card border-top accent ungu, custom `.drop-area`, `.file-item`, gradient button
- [ ] Gunakan CSS custom properties `--accent: #4f46e5` dan `--accent-dark: #3730a3`

```html
    <style>
        :root {
            --accent: #4f46e5;
            --accent-dark: #3730a3;
        }
        body {
            min-height: 100vh;
            background: linear-gradient(135deg, #eef2ff 0%, #f8faff 60%, #e0e7ff 100%);
            display: flex;
            align-items: center;
        }
        .upload-container {
            max-width: 560px;
            width: 100%;
            margin: 40px auto;
            background: #fff;
            padding: 36px 40px;
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(79,70,229,0.10), 0 1.5px 6px rgba(0,0,0,0.06);
            border-top: 4px solid var(--accent);
        }
        .upload-title {
            font-weight: 700;
            font-size: 1.4rem;
            color: #1e1b4b;
            letter-spacing: -0.02em;
        }
        .upload-title span { color: var(--accent); }
        .drop-area {
            border: 2px dashed #c7d2fe;
            border-radius: 12px;
            padding: 28px 16px;
            text-align: center;
            cursor: pointer;
            transition: border-color 0.2s, background 0.2s;
            background: #f5f3ff;
            position: relative;
        }
        .drop-area:hover, .drop-area.dragover {
            border-color: var(--accent);
            background: #eef2ff;
        }
        .drop-area input[type="file"] {
            position: absolute;
            inset: 0;
            opacity: 0;
            cursor: pointer;
            width: 100%;
            height: 100%;
        }
        .drop-icon { font-size: 2rem; margin-bottom: 8px; display: block; }
        .drop-label { color: #6b7280; font-size: 0.92rem; }
        .drop-label strong { color: var(--accent); }
        .file-item {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 6px 10px;
            border-radius: 8px;
            background: #f5f3ff;
            font-size: 0.85rem;
            color: #374151;
            margin-bottom: 4px;
        }
        .file-item .file-name { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
        .file-item .file-size { color: #9ca3af; font-size: 0.78rem; flex-shrink: 0; }
        #uploadBtn {
            background: linear-gradient(90deg, var(--accent) 0%, var(--accent-dark) 100%);
            border: none;
            border-radius: 10px;
            font-weight: 600;
            transition: opacity 0.2s, box-shadow 0.2s;
            box-shadow: 0 2px 8px rgba(79,70,229,0.18);
        }
        #uploadBtn:hover:not(:disabled) { opacity: 0.92; box-shadow: 0 4px 16px rgba(79,70,229,0.28); }
        #uploadBtn:disabled { opacity: 0.7; }
    </style>
```

---

### Task 2: Update markup H2 + form

**Files:** `templates/upload_form.html:30`, `templates/upload_form.html:40-54`

- [ ] Ganti `<h2>` (baris 30): tambah class `upload-title` dan bungkus "Watermark" dalam `<span>`
- [ ] Ganti blok form (baris 40-54): input file jadi custom `.drop-area`, tambah `<div id="file-list">` di bawahnya

H2 baru:
```html
            <h2 class="upload-title text-center mb-4">PDF <span>Watermark</span> Tool</h2>
```

Form baru:
```html
            <form method="post" action="/upload" enctype="multipart/form-data" id="uploadForm">
                <div class="mb-4">
                    <label class="form-label fw-semibold text-secondary small text-uppercase mb-2">
                        Pilih atau drag &amp; drop file
                    </label>
                    <div class="drop-area" id="dropArea">
                        <input type="file" name="pdf_files" id="pdf_files" multiple required
                            accept=".pdf,.jpg,.jpeg,.png">
                        <span class="drop-icon">&#128196;</span>
                        <p class="mb-1"><strong>Klik untuk pilih</strong> atau drop file di sini</p>
                        <p class="drop-label mb-0">Format: PDF, JPG, PNG &mdash; bisa banyak file</p>
                    </div>
                    <div id="file-list" class="mt-2"></div>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-primary btn-lg" id="uploadBtn">
                        <span class="btn-text">Mulai Proses</span>
                        <span class="spinner-border spinner-border-sm d-none" role="status"></span>
                    </button>
                </div>
            </form>
```

---

### Task 3: Tambah JS file list preview + drag visual

**Files:** `templates/upload_form.html` — blok `<script>` yang sudah ada

- [ ] Tambahkan `renderFileList()`, event listener `change` pada input, dan drag-over visual — **di dalam** `<script>` yang sudah ada, setelah deklarasi `const spinner`, sebelum `form.addEventListener`

```js
        // File list preview
        function renderFileList(files) {
            const list = document.getElementById('file-list');
            if (!files.length) { list.innerHTML = ''; return; }
            list.innerHTML = Array.from(files).map(f => {
                const kb = (f.size / 1024).toFixed(1);
                return `<div class="file-item">
                    <span>&#128196;</span>
                    <span class="file-name">${f.name}</span>
                    <span class="file-size">${kb} KB</span>
                </div>`;
            }).join('');
        }
        document.getElementById('pdf_files').addEventListener('change', function () {
            renderFileList(this.files);
        });
        const dropArea = document.getElementById('dropArea');
        dropArea.addEventListener('dragover', e => { e.preventDefault(); dropArea.classList.add('dragover'); });
        dropArea.addEventListener('dragleave', () => dropArea.classList.remove('dragover'));
        dropArea.addEventListener('drop', () => dropArea.classList.remove('dragover'));
```

---

## Self-Review

- [x] Modernisasi tampilan: gradient bg, card accent, drop area visual ✓
- [x] Minor polish — tidak ganti struktur, tidak tambah lib ✓
- [x] Logic flash, SweetAlert, form submit tidak berubah ✓
- [x] `name="pdf_files"` dan `accept` attribute tidak berubah ✓
- [x] Responsive tetap via Bootstrap ✓
